#include <bits/stdc++.h>
#define endl '\n'
#define IOS ios_base::sync_with_stdio(false); cin.tie(NULL);

using namespace std;

int main(){
	//IOS;
	vector<int>a;
	int x;
	while(cin >> x && x != 42){
		cout << x << '\n';
	}
	return 0;
}

