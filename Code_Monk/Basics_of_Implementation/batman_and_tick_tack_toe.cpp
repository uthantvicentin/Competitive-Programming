#include <bits/stdc++.h>

using namespace std;

int main(){
	string m[4];
	int n;
	cin >> n;
	cin.ignore();
	while(n--){
		for(int i = 0 ; i < 4 ; ++i)
			getline(cin,m[i]);

		bool flag = false;
		for(int i = 0 ; i < 4 ; ++i)
			for(int j = 0 ; j < 4 ; ++j){
				if(m[i][j] == '.')
			}
		}



	} 

	return 0;
}
