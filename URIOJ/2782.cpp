#include <bits/stdc++.h>
#define endl '\n'
#define IOS ios_base::sync_with_stdio(false); cin.tie(NULL);

using namespace std;

int main(){
	int n;
	scanf("%d",&n);
	int a[n+5],aux[n];
	int cont = 0;
	int sum = 0;
	if(n == 1 || n == 2){
		printf("1\n");
		return;
	}	
	scanf("%d",a[0]);
	for(int i = 1 ; i < n ; ++i){
		scanf("%d",a[i]);
		sum = abs(a[i-1] - abs[i-])
		if(a[i] > 106 || a[i] < -106)
			break;
	}

	if(i == 1 || i == 2)
		printf("1\n");

	return 0;
}

