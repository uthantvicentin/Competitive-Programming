#include <bits/stdc++.h>
#define endl '\n'
#define IOS ios_base::sync_with_stdio(false); cin.tie(NULL);

using namespace std;

int main(){
	IOS;
	int n,m,a[100005];
	while(cin >> n >> m and n){
		memset(a,0,sizeof(a));
		for(int i = 0 ; i < n ; ++i) cin >> a[i];
		int q,a,b;
		cin >> q >> a >> b;
		if(q == 1){


		}
		else{

		}

	}

	return 0;
}

